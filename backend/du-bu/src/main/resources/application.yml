spring:
  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
    defer-datasource-initialization: true

  datasource:
    username: root
    password: root
    url: jdbc:mariadb://j9a306.p.ssafy.io:3306/dubu
    driver-class-name: org.mariadb.jdbc.Driver

#  sql:
#    init:
#      mode: never

server:
  port: 8080
